import { convertBase64, uploadImage } from "./helpers";
import {Background} from "../Constants/HomePagesBackground"

export default function SignUpPage({loading, setLoading}) {
    const [url, setUrl] = useState(null);
    return (
        <>
        <Background>
            
        </Background>
        </>
    )
}